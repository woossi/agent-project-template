{
  "_comment": "예시 서버 정의 조각입니다. 실제 사용 시 이 파일을 복사해 <이름>.json 으로 만들고 루트 .mcp.json 에 병합하세요. (이 파일 자체는 .mcp.json 에 등록되지 않습니다.)",
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"],
    "env": {}
  }
}
