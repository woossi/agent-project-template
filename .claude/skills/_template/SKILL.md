# 스킬: <이름>

> 이 폴더(`skills/_template/`)는 새 스킬을 만들 때 복사하는 본보기입니다.
> 복사 → 폴더 이름을 `<스킬이름>`(kebab-case)으로 변경 → 아래 내용을 채우고
> 저장하면 프로젝트 훅이 `skills/skills.md`의 영어 색인을 자동 갱신합니다.

## 사용 시점

이 스킬을 적용해야 하는 트리거 조건.

## 목적

이 스킬이 반복적으로 달성하는 목표.

## 입력

- 필요한 입력 (파일, 데이터, 파라미터 등)

## 절차

1. 단계
2. 내부 코드를 쓰는 단계는 폴더 기준 상대경로로 참조합니다. 예: `python scripts/run.py`
3. 단계

## 출력 형식

기대하는 결과의 형식.

## 내부 자원

이 폴더 안의 모든 파일과 하위 폴더를 빠짐없이 나열합니다. (명시되지 않은 떠도는 파일 금지)

- `scripts/run.py` — 절차의 핵심 실행 코드(예시)
- `references/notes.md` — 참고 메모·배경 설명(예시)

## 품질 점검

- 출력이 올바른지 확인하는 검증 규칙

## 자주 발생하는 실패 사례

- 실패 양상 → 해결 방법
